# Copy to application-local.conf and customize, or set env vars (they take priority).
database {
    jdbcUrl = "jdbc:postgresql://localhost:5432/leetcode_tracker"
    user = "tracker"
    password = "tracker"
}

jwt {
    secret = "dev-only-secret-change-me-please-use-32chars-minimum!!"
}
